/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./*.{html,js}"],
    theme: {
        extend: {
            colors: {
                base: "#1B1A17",
                gold: "#F0A500"
            }
        },
    },
    plugins: [],
}